The most helpful aspect of this code's structure was the spacing between functions, with clear labels defining the function's purpose. This made it 
easy to separate them out mentally, keeping them clear as different blocks of code. 

I was slightly confused by the mostReadBook function, because it took me a few minutes to realize that the bottom piece (starting with "let maxBook")
was also part of the same function and not a new block of code. Once I took a few seconds to sort out and match up all the curly brackets, I realized 
that it was also part of the same function. Then, it became clear that the top half is JUST for finding the total count of each book, whereas the 
second half actually compares those counts against each other to find the highest one. 
